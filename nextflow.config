profiles {
    local {

    }

    sge {
        process.executor = 'sge'
        process.queue = 'mgi_supermem.q'
        process.cpus = 6
        process.memory = '500GB'
        process.penv = 'cg'
        process.clusterOptions = '-cwd -l vf=500G,num_proc=6 -P P21Z18000N0016'
    }
}